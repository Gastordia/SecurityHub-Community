"""
Django app configuration for utils
"""
from django.apps import AppConfig


class UtilsConfig(AppConfig):
    """Configuration for the utils app"""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'utils'
    verbose_name = 'Utilities'










